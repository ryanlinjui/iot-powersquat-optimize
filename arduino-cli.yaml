board_manager:
    additional_urls: [
        "https://static-cdn.m5stack.com/resource/arduino/package_m5stack_index.json"
    ]

directories:
    builtin.libraries: "./M5StickC/lib"

library:
    enable_unsafe_install: true